'use strict';

// ─────────────────────────────────────────────────────────────────────────────
// Example Controller — rename / duplicate for each feature you add.
//
// A controller function:
//   1. Reads data from req (params, query, body)
//   2. Calls a service / runs a query
//   3. Sends a structured JSON response
//   4. Forwards unexpected errors to next(err) so errorHandler picks them up
//
// Suggested pattern for async controllers:
//
//   const getAll = async (req, res, next) => {
//     try {
//       // TODO: call your service / run your SQL query here
//       res.status(200).json({ success: true, data: [] });
//     } catch (err) {
//       next(err);
//     }
//   };
// ─────────────────────────────────────────────────────────────────────────────

// TODO: Import the database pool when you're ready to run queries.
// const { pool } = require('../config/database');

// TODO: Implement your controller functions below and export them.

module.exports = {};
